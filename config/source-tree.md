# Estrutura de Código Fonte (Source Tree)

Visão geral da estrutura de diretórios do código-fonte a ser criado por `@dev-fullstack`.

```
/
├── packages/
│   ├── web/          # Aplicação React (Página de Vendas, Área de Membros)
│   │   ├── src/
│   │   └── package.json
│   └── api/          # Servidor Node.js (API REST)
│       ├── src/
│       └── package.json
├── package.json      # Raiz do monorepo (Yarn/NPM workspaces)
└── README.md
```
